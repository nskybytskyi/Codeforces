#include <iostream>

int main() {
  std::ios::sync_with_stdio(false);
  std::cin.tie(nullptr);
  std::cout.tie(nullptr);

  // std::freopen("comb.in", "r", stdin);
  // std::freopen("comb.out", "w", stdout);

  int length, choose; int64_t index;
  std::cin >> length >> choose >> index;

  // todo

  return 0;
}
